// roots problem
#include <stdio.h>
#include<math.h>
int main() {
   float a,b,c;
   scanf("%f %f %f",&a,&b,&c);
   float d=b*b-4*a*c;//to find whether the roots are real or imaginary
   if(d<0){
       printf("Imaginary number roots are not possible");
   }
   else{
   float r1=(-b+sqrt(b*b-4*a*c))/(2*a);
   float r2=(-b-sqrt(b*b-4*a*c))/(2*a);
    printf("%.1f\n",r1);
    printf("%.1f\n",r2);
   }
    return 0;
}
